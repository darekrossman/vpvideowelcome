# React Playground

A simple React application scaffold for experimenting with React components.

Install it
----------
1. Clone the repo
2. Install the deps
  `npm install`
3. Start it up
  `gulp watch`
